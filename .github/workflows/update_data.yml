name: Update Ticker Data

on:
  schedule:
    - cron: '0 0 * * *' # Esegue ogni giorno a mezzanotte UTC
  workflow_dispatch: # Permette l'avvio manuale dalla scheda Actions

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Essenziale per permettere a GitHub di salvare i file CSV nel repo

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install yfinance pandas

    - name: Run download script
      run: python download_data.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        # Aggiungiamo tutta la cartella data, incluse le sottocartelle come ETF/
        git add data/
        git commit -m "Aggiornamento dati storici ETF $(date)" || echo "Nessuna modifica da caricare"
        git push
